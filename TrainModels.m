% Train models based on training data

% % Load data

% Label Data based on the number using OCR

% Normalising the faces to a 200x200 box and then extracting consistent 
% features is one way to handle this. (J = imresize(I, [N, M]);)

% Train models
% Use (at least) three different classification methods 
% (such as CNN, SVM, MLP, etc.) to recognise individuals in an image.
% For each classification method, use (at least) two different types 
% of features (e.g., SURF, HOG, etc.) as applicable.
% (30%: 5% for each classifier-feature combination).


% Return Number and Location